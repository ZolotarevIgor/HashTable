<Page x:Class="Hash.HashPr2" 
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
d:DesignHeight="300" d:DesignWidth="300" 
Title="HashPr2">






    <Grid>
       

        <ListView x:Name="HashTableList" HorizontalAlignment="Stretch" ItemsSource="{Binding}" 
                  BorderBrush="{x:Null}" Foreground="{x:Null}" HorizontalContentAlignment="Center"
                  Background="{x:Null}" Padding="5,0">
            <ListView.ItemContainerStyle >
                <Style TargetType="ListViewItem">
                    <Setter Property="Focusable" Value="False" />
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

    </Grid>
</Page>